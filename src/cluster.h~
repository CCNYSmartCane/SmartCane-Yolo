#ifndef CLUSTER_H
#define CLUSTER_H

#include <stdlib.h>
#include <stdio.h>
#include <float.h>
#include <string.h>
#include <math.h>
#include "darknet.h"
#include <stdlib.h>
#include <stdio.h>

typedef struct{
    box* frame1;
    size_t size1;

    box* frame2;
    size_t size2;

    box* frame3;
    size_t size3;
    
    box* frame4;
    size_t size4;

    box* frame5;
    size_t size5;
    
    box* frame6;
    size_t size6;

    box* frame7;
    size_t size7;
    
    box* frame8;
    size_t size8;

    box* frame9;
    size_t size9;

    box* frame10;
    size_t size10;

    size_t init; 
}framewindow;

typedef struct{
    char name;
    box*
 detections;
}centroid;

double* cluster(framewindow input, FILE *fp, FILE *fp2);

#endif
